on:
  workflow_dispatch: 
  release:
    types: [released]

jobs:
  dump_contexts_to_log:
    runs-on: ubuntu-latest
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github.even) }}
        run: echo '$GITHUB_CONTEXT'
    
